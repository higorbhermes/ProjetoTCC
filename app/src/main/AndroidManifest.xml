<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="higorhermes.tcc.projetotcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Qualificando"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.TelaMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.TelaQuiz" />
        <activity android:name=".View.TelaEscolherAssuntoJogo" />
        <activity android:name=".View.TelaDesempenho" />
        <activity android:name=".View.TelaEscolherAssuntoCaderno" />
        <activity android:name=".View.TelaSobre" />
        <activity android:name=".View.TelaAcerto"></activity>
    </application>

</manifest>